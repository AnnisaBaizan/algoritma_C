#include <stdio.h>

int main() {
    int a = 10;
    
    printf("Initial value: %d\n", a); // Nilai awal    
    a++; // Increment
    printf("After increment: %d\n", a); 
        a--; // Decrement
    printf("After decrement: %d\n", a);
    
    return 0;
} 